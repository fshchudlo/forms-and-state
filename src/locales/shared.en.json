{
    "buttons": {
        "send": "Send"
    },
    "validation": {
        "maxlength": "Maximum length is {{requiredLength}}",
        "ьштlength": "Minimum length is {{requiredLength}}",
        "required": "Field is required"
    }
}
